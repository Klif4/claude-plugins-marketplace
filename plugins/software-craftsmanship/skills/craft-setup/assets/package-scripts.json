{
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:acceptance": "NODE_OPTIONS='--import tsx' cucumber-js",
    "coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "build": "vite build"
  }
}
